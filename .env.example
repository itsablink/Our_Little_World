# Authentication (Website Password Gate)
LOGIN_ID=
LOGIN_PASSWORD=
SESSION_SECRET=

# Database Integration (Postgres / Vercel KV / Supabase for persistent Diary & Message metadata)
DATABASE_URL=

# Persistent Object Storage (Vercel Blob for Message Memory screenshots)
BLOB_READ_WRITE_TOKEN=
